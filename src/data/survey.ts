/**
 * 설문 전체 모양 설정.
 *
 * 여기 숫자는 사람이 정한 목표치다 (임시값 아님).
 * 문항이 만들어지면 이 값에 맞춰 출제기가 뽑는다.
 */
export const SURVEY = {
  /** 전체 문항 상한. 시작 질문 + 스토리 문항을 합쳐서 */
  maxTotal: 120,

  /** 스토리 구간에서 낼 문항 수 목표 */
  storyTarget: 100,

  /**
   * 역할(aspect)별 최소 문항 수.
   * 자신감·적극성 같은 항목이 골고루 나와야 "제대로 검사했다"고 할 수 있다.
   * 출제기가 이 수를 먼저 채운 뒤 나머지를 무작위로 채운다.
   */
  minPerAspect: 6,

  /**
   * 한 장면에서 연속으로 낼 문항 수의 상한.
   * 넘으면 지루해진다. 지문·대사가 사이에 끼도록 유도하는 값이다.
   */
  maxPerScene: 8,
} as const;
